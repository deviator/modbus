{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug with serialport",
            "type": "gdb",
            "request": "launch",
            "target": "./modbus-test-serialport",
            "cwd": "${workspaceRoot}"
        }
    ]
}